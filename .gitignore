# Local secrets — the installer writes this; the example template is committed.
.env
shim/.env

# Local logs from the shim's unattended runs.
*.log
shim/ship.log

# Python venv / build artifacts
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Helm dependency archives — resolved from Chart.lock by `helm dependency
# build`, so the tarball itself is not committed. Chart.lock IS committed.
charts/*/charts/*.tgz

# Editor scratch
.vscode/
.idea/
*.swp

# Local-only Claude Code settings (permissive allowlist; never commit/publish)
.claude/settings.local.json
